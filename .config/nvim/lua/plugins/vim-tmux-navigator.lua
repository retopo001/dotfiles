return {
  "christoomey/vim-tmux-navigator",
  lazy = false,
  -- Let the plugin set up its own mappings automatically
  -- It will handle forwarding to tmux when at the edge of Neovim splits
}

