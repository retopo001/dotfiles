#!/bin/bash
# Toggle redshift nightlight (X11)

if pgrep -x redshift >/dev/null; then
    pkill redshift
    notify-send "Daylight screen temperature"
else
    redshift -O 4000 &
    notify-send "Nightlight screen temperature"
fi
